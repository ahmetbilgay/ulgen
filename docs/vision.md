# Vision

ULGEN aims to be a sovereign infrastructure control center with a local-first operating model.

The user owns credentials, state, and execution paths by default. Hosted services are optional additions, not requirements.
