export function TerminalPanel() {
  return (
    <section className="panel terminal">
      <h2>Terminal</h2>
      <p>SSH tunnel orchestration and terminal streaming will attach here from `ulgen-core::ssh`.</p>
    </section>
  );
}
